import React from 'react';
import Ajout_contents from '../components/Ajout_contents';

const Ajout_taches = () => {
    return (
      <div className="Ajout d-flex align-items-center justify-content-center">
        <Ajout_contents />
      </div>
    );
};

export default Ajout_taches;